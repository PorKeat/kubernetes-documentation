# PV Documenation

## Access Mode

- ReadWriteOnce (RWO): volume can be mounted as read and write by only one node at a time
- ReadOnlyMany (ROX): volume can be mounted as read-only by many nodes at the same time
- ReadWriteMany (RWX): volume can be mounted as read and write by many nodes at the same time
